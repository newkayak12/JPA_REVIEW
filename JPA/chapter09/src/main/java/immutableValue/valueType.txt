값 타입을 공유하면 공유하고 있는 하나의 객체해서 해당 값을 수정하면 다른 공유하고 있는 객체의 값 역시 변경된다. 특히 참조형 자료형을 사용하거나 임베디드의 경우에 말이다.

    객체의 공유 참조는 간간히 있다. 따라서 근본적인 해결책이 필요한데, 가장 단순한 방법은 객체의 값을 수정하지 못하게 하는 것이다.

    예를 들어서 setter를 없애는 식으로 말이다. 혹은 객체 자체를 불변하게 만들면 값을 수정할 수 없다. 그에 따라 부작용을 막을 수 있다. 따라서 값 타입은 되도록 불변 객체로 설계해야한다.

