CascadeType.ALL : 아래의 모든 내용을 적용한다.
           .PERSIST : 엔티티 영속화할 때 필드에 보유된 엔티티도 영속성 컨텍스트로 관리함
           .DETACH : 부모엔티티가 영속성 컨텍스트에서 나올 떄 자식도 함께 나옴
           .REMOVE : 엔티티를 삭제할 때 필드에 보유한 엔티티도 삭제
           .MERGE : 엔티티 상태를 병합할 때, 필드에 보유된 엔티티도 병합 (?)
           .REFRESH : 엔티티를 새로 고칠 때 필드에 보유된 엔티티도 새로 고친다.
