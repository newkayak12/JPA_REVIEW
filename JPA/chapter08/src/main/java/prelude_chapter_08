chapter08
- 프록시(디자인패턴의?)와 지연 로딩, 즉시 로딩

    객체 그래프 탐색 중 연관된 객체를 마음대로 탐색하는 것은 어렵다. 이러한 문제점을 해소하기 위해서 PROXY라는 기술을 이용하여
연관된 객체를 처음부터 DB에서 조회하는 것이 아니라 실제 사용하는 시점에서 조회할 수 있다.

- 영속성 전이와 고아 객체

        JPA는 연관된 객체를 함께 저장하거나 함께 삭제할 수 이는 영속성 전이와 고아 객체 제거라는 편리한 기능을 제공한다.(DB에서의 Cascade 옵션)