select 절에 조회할 대상을 지정하는 것을 프로젝션이라고 하고 SELECT {프로젝션 대상} FROM으로 대상을 선택한다.
프로젝션 대상은 엔티티, 임베디드 타입, 스칼라 타입(숫자, 문자 등 기본 자료형)이 있다.

프로젝션으로 조회한 엔티티는 영속성 컨텍스트의 관리를 받는다.

추가적으로 임베디드 타입은 조회의 시작점이 될 수 없기 때문에 이를 포함하고 있는 엔티티를 조회하는 식으로 가져와야한다.